[package]
name = "doc-parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "doc_parser"
path = "src/lib.rs"

[dependencies]
thiserror = "1.0"
# Using pulldown-cmark initially for AST; can swap later
pulldown-cmark = { version = "0.9", default-features = false }
